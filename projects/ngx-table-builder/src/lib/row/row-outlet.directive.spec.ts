import { RowOutletDirective } from './row-outlet.directive';

describe('RowOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new RowOutletDirective();
    expect(directive).toBeTruthy();
  });
});
