import { CellOutletDirective } from './cell-outlet.directive';

describe('CellOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new CellOutletDirective();
    expect(directive).toBeTruthy();
  });
});
